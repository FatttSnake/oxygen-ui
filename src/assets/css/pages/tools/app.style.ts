import { createStyles } from 'antd-style'

export default createStyles(() => ({
    root: {
        padding: 20,
        alignItems: 'center'
    },

    getItOnGooglePlay: {
        width: '50vw',
        maxWidth: 400
    }
}))
