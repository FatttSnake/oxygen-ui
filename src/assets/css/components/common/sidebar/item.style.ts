import { createStyles } from 'antd-style'

export default createStyles(() => ({
    item: {
        position: 'relative',
        fontSize: '1rem'
    },

    menuBt: {
        borderRadius: '8px',
        overflow: 'hidden',
        height: '40px'
    }
}))
